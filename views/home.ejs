<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to ProjectHunter</title>
    <!-- Assuming you have a main CSS file or are using Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
        }
        .navbar .nav-link {
            font-weight: 500;
        }
        .navbar .dropdown-menu {
            border-radius: 0.5rem;
            border: none;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
        }
        .navbar .dropdown-item {
            font-weight: 500;
        }
        .navbar .dropdown-item:hover {
            background-color: #f8f9fa;
        }

        /* Hero Section Styles */
        .hero-section {
            background-color: #212529; /* Dark background */
            color: white;
            padding: 100px 0;
            text-align: center;
        }
        .hero-section h1 {
            font-size: 4rem;
            font-weight: 700;
            position: relative;
        }
        /* Cool text overlay effect */
        .hero-section h1::before {
            content: "Find Your Next Team";
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
            color: rgba(255, 255, 255, 0.1);
            font-size: 4.5rem;
            font-weight: 700;
            z-index: 1;
        }
        .hero-section .lead {
            font-size: 1.5rem;
            color: #adb5bd;
            margin-bottom: 30px;
            position: relative;
            z-index: 2;
        }
        .hero-section .btn {
            font-size: 1.1rem;
            font-weight: 600;
            padding: 12px 30px;
            border-radius: 50px;
            margin: 0 10px;
            transition: all 0.3s ease;
        }
        .btn-find {
            background-color: #0d6efd;
            border-color: #0d6efd;
            color: white;
        }
        .btn-find:hover {
            background-color: #0b5ed7;
            border-color: #0a58ca;
            transform: translateY(-2px);
        }
        .btn-create {
            background-color: #198754;
            border-color: #198754;
            color: white;
        }
        .btn-create:hover {
            background-color: #157347;
            border-color: #146c43;
            transform: translateY(-2px);
        }

        /* Features Section Styles */
        .features-section {
            padding: 80px 0;
            background-color: #f8f9fa;
        }
        .feature-card {
            text-align: center;
            padding: 20px;
        }
        .feature-card i {
            font-size: 3rem;
            color: #0d6efd;
            margin-bottom: 20px;
        }
        .feature-card h3 {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 15px;
        }
        .feature-card p {
            color: #495057;
        }
    </style>
    <!-- Include a font icon library like Bootstrap Icons or Font Awesome for the i tags -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
</head>
<body>

    <!-- This partial will include your updated navbar -->
    <%- include('./partials/navbar') %>

    <!-- Hero Section -->
    <div class="hero-section">
        <div class="container">
            <h1>Find Your Next Project</h1>
            <p class="lead">Connect with creators and build amazing things together.</p>
        </div>
    </div>

    <!-- Features Section -->
    <div class="features-section">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <div class="feature-card">
                        <i class="bi bi-search"></i>
                        <h3>Discover</h3>
                        <p>Find projects that match your skills and interests, from Web Development to AI/ML.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-card">
                        <i class="bi bi-people"></i>
                        <h3>Collaborate</h3>
                        <p>Apply to join teams and contribute to exciting new ventures.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-card">
                        <i class="bi bi-lightbulb"></i>
                        <h3>Create</h3>
                        <p>Have an idea? Post your own project and recruit talented collaborators.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <%- include('./partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

