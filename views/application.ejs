<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Applications</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: radial-gradient(circle at top center, #0a0a0a, #000 60%, #050505 100%);
      color: #fff;
      min-height: 100vh;
      overflow-x: hidden;
    }

    h2 {
      font-weight: 600;
      color: #00bfff;
      text-align: center;
      text-shadow: 0 0 10px rgba(0, 191, 255, 0.4);
      margin-bottom: 1rem;
    }

    p.text-muted {
      color: #a5a5a5 !important;
      text-align: center;
    }

    .project-card {
      background: rgba(20, 20, 20, 0.8);
      border: 1px solid rgba(0, 191, 255, 0.2);
      border-radius: 15px;
      box-shadow: 0 0 20px rgba(0, 191, 255, 0.1);
      transition: all 0.3s ease;
      animation: fadeIn 0.8s ease;
      backdrop-filter: blur(10px);
    }

    .project-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 0 25px rgba(0, 191, 255, 0.3);
    }

    .card-body h5 {
      color: #00bfff;
    }

    .card-body h6 {
      color: #aaa !important;
    }

    .status-badge {
      font-size: 0.9rem;
      padding: 6px 10px;
      border-radius: 8px;
    }

    .badge.bg-warning {
      background: linear-gradient(90deg, #ffb347, #ffcc33) !important;
      color: #000 !important;
    }

    .badge.bg-danger {
      background: linear-gradient(90deg, #ff4b2b, #ff416c) !important;
    }

    .card-footer p {
      color: #999;
    }

    .empty-box {
      background: rgba(15, 15, 15, 0.8);
      border: 1px solid rgba(0, 191, 255, 0.15);
      backdrop-filter: blur(8px);
      color: #fff;
      border-radius: 20px;
      transition: 0.3s;
      box-shadow: 0 0 25px rgba(0, 191, 255, 0.1);
    }

    .empty-box:hover {
      box-shadow: 0 0 30px rgba(0, 191, 255, 0.3);
    }

    .btn-primary {
      background: linear-gradient(90deg, #00bfff, #0077ff);
      border: none;
      color: #fff;
      font-weight: 500;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0, 191, 255, 0.4);
      transition: all 0.4s ease;
    }

    .btn-primary:hover {
      background: linear-gradient(90deg, #0077ff, #00bfff);
      box-shadow: 0 0 25px rgba(0, 191, 255, 0.6);
      transform: scale(1.05);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body class="pb-5">
  <%- include('partials/navbar') %>

  <div class="container mt-5">
    <h2>My Pending Applications</h2>
    <p class="text-muted mb-4">This page shows your applications from the last 3 days that are still open.</p>

    <div class="row row-cols-1 row-cols-md-2 g-4">
      <% if (projects && projects.length > 0) { %>
        <% projects.forEach(project => { %>
          <div class="col">
            <div class="project-card h-100 p-3">
              <div class="card-body">
                <div class="d-flex justify-content-between align-items-start">
                  <div>
                    <h5 class="card-title fw-bold"><%= project.title %></h5>
                    <h6 class="card-subtitle mb-2"><%= project.domain %></h6>
                  </div>
                  <% if (project.applicationStatus === 'Pending') { %>
                    <span class="badge bg-warning text-dark status-badge">Pending</span>
                  <% } else { %>
                    <span class="badge bg-danger status-badge">Rejected</span>
                  <% } %>
                </div>
                <p class="card-text mt-2"><%= project.desc %></p>
                <p class="card-text"><small class="fw-bold text-light">
                  Skills: <%= (project.skills_req && typeof project.skills_req.join === 'function') ? project.skills_req.join(', ') : (project.skills_req || 'None specified') %>
                </small></p>
              </div>
              <div class="card-footer bg-transparent border-0 pt-0">
                <p class="mb-0 small">Project Owner: <%= project.createdBy.name || project.createdBy.email %></p>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <div class="col-12">
          <div class="text-center p-5 empty-box">
            <h4 class="text-light">You have no pending applications.</h4>
            <p class="text-light">Find a project to apply for!</p>
            <a href="/project/find" class="btn btn-primary">Find Projects</a>
          </div>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
