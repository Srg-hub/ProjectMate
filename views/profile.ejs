<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #0d0d0d;
            color: #f1f1f1;
            min-height: 100vh;
        }

        .profile-card {
            background-color: #1a1a1a;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.4);
            max-width: 600px;
            margin: 4rem auto;
            overflow: hidden;
            animation: softGlow 4s ease-in-out infinite alternate;
        }

        @keyframes softGlow {
            from { box-shadow: 0 0 15px rgba(0, 150, 255, 0.3); }
            to { box-shadow: 0 0 25px rgba(0, 150, 255, 0.6); }
        }

        .profile-header {
            background-color: #007bff;
            color: white;
            padding: 3rem 2rem;
            text-align: center;
        }

        .profile-header h2 {
            margin: 0;
            font-weight: 700;
            font-size: 1.8rem;
        }

        .profile-body {
            padding: 2rem;
        }

        .info-group {
            margin-bottom: 1.5rem;
        }

        .info-group label {
            font-weight: 600;
            color: #00aaff;
            display: block;
            margin-bottom: 0.25rem;
            font-size: 1rem;
        }

        .info-group p {
            font-size: 1.1rem;
            color: #f1f1f1;
            margin: 0;
            word-break: break-all;
        }

        .text-muted small {
            color: #b5b5b5 !important;
        }

        .btn-danger {
            background-color: #e63946;
            border: none;
            color: #fff;
            font-weight: 600;
            padding: 12px;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .btn-danger:hover {
            background-color: #ff4757;
            transform: scale(1.03);
        }
    </style>
</head>
<body class="pb-5">
    <%- include('partials/navbar') %>

    <div class="container">
        <div class="profile-card">
            <div class="profile-header">
                <h2>My Profile</h2>
            </div>
            <div class="profile-body">
                <div class="info-group">
                    <label>Name</label>
                    <p><%= user.name %></p>
                </div>
                <div class="info-group">
                    <label>Email</label>
                    <p><%= user.email %></p>
                </div>
                <div class="info-group">
                    <label>User ID</label>
                    <p class="text-muted"><small><%= user._id %></small></p>
                </div>
                <a href="/user/logout" class="btn btn-danger w-100 mt-3">Logout</a>
            </div>
        </div>
    </div>
</body>
</html>
