<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Your Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap');

    body {
      font-family: 'Inter', sans-serif;
      background: radial-gradient(circle at top left, #0d1117, #010409);
      color: #e6edf3;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Header */
    .dashboard-header {
      background: linear-gradient(120deg, #161b22, #0d1117);
      color: #fff;
      text-align: center;
      padding: 60px 20px 40px;
      position: relative;
    }

    .dashboard-header::after {
      content: "";
      position: absolute;
      inset: 0;
      background: radial-gradient(circle at 50% 100%, rgba(88,166,255,0.15), transparent 70%);
      pointer-events: none;
    }

    .dashboard-header h1 {
      font-weight: 700;
      font-size: 2.5rem;
      margin-bottom: 0;
    }

    /* Grid */
    .dashboard-grid {
      padding: 70px 0;
      position: relative;
    }

    /* Subtle floating animation background */
    .floating-blur {
      position: absolute;
      width: 200px;
      height: 200px;
      background: #58a6ff;
      filter: blur(100px);
      border-radius: 50%;
      opacity: 0.1;
      animation: float 12s ease-in-out infinite alternate;
      z-index: 0;
    }
    .floating-blur:nth-child(1) { top: 20%; left: 10%; }
    .floating-blur:nth-child(2) { top: 60%; right: 10%; animation-delay: 6s; }

    @keyframes float {
      0% { transform: translateY(0); }
      100% { transform: translateY(60px); }
    }

    /* Cards */
    .nav-card {
      position: relative;
      background: rgba(22,27,34,0.75);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(240,246,252,0.1);
      border-radius: 16px;
      padding: 30px;
      text-align: center;
      color: #e6edf3;
      text-decoration: none;
      display: flex;
      flex-direction: column;
      justify-content: center;
      height: 100%;
      transition: all 0.35s ease;
      overflow: hidden;
      box-shadow: 0 0 0 rgba(0,0,0,0);
      z-index: 1;
    }

    .nav-card:hover {
      transform: translateY(-10px) scale(1.02);
      box-shadow: 0 15px 40px rgba(56,139,253,0.2);
      border-color: rgba(88,166,255,0.3);
    }

    .nav-card-icon {
      font-size: 3rem;
      color: #58a6ff;
      margin-bottom: 15px;
      transition: transform 0.4s ease;
    }

    .nav-card:hover .nav-card-icon {
      transform: rotate(5deg) scale(1.2);
    }

    .nav-card h3 {
      font-weight: 600;
      font-size: 1.5rem;
      margin-bottom: 10px;
      color: #e6edf3;
    }

    .nav-card p {
      color: #8b949e;
      font-size: 0.95rem;
      line-height: 1.6;
    }

    /* Hover gradient overlay */
    .nav-card::before {
      content: "";
      position: absolute;
      inset: 0;
      background: radial-gradient(circle at top, rgba(88,166,255,0.15), transparent 70%);
      opacity: 0;
      transition: opacity 0.4s ease;
      z-index: -1;
    }

    .nav-card:hover::before {
      opacity: 1;
    }

    /* Footer */
    footer {
      text-align: center;
      padding: 20px;
      color: #8b949e;
      background-color: #0d1117;
      border-top: 1px solid #30363d;
    }
    :root {
      --font-main: 'IBM Plex Sans', sans-serif;
      --font-mono: 'IBM Plex Mono', monospace;
      --color-bg: #0d1117;
      --color-surface: rgba(22,27,34,0.75);
      --color-border: rgba(240,246,252,0.1);
      --color-text: #e6edf3;
      --color-subtext: #8b949e;
      --color-accent: #58a6ff;
    }

    body {
      font-family: var(--font-main);
      background: radial-gradient(circle at top left, var(--color-bg), #010409);
      color: var(--color-text);
      min-height: 100vh;
      overflow-x: hidden;
    }

    h1, h2, h3, h4, h5, h6 {
      font-family: var(--font-main);
      font-weight: 600;
    }

    code, pre, .code-text {
      font-family: var(--font-mono);
      background: rgba(240, 246, 252, 0.05);
      padding: 2px 6px;
      border-radius: 4px;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <%- include('./partials/navbar') %>

  <!-- Header -->
  <header class="dashboard-header">
    <div class="container">
      <h1>Welcome <%= user.name.split(' ')[0] %> 👋</h1>
    </div>
  </header>

  <!-- Animated Blurs -->
  <div class="floating-blur"></div>
  <div class="floating-blur"></div>

  <!-- Dashboard Grid -->
  <main class="dashboard-grid">
    <div class="container">
      <div class="row g-4">

        <div class="col-lg-4 col-md-6">
          <a href="/project/find" class="nav-card">
            <div class="nav-card-icon">🔍</div>
            <h3>Find Projects</h3>
            <p>Explore projects by other creators and join teams you vibe with.</p>
          </a>
        </div>

        <div class="col-lg-4 col-md-6">
          <a href="/project/create" class="nav-card">
            <div class="nav-card-icon">➕</div>
            <h3>Create Project</h3>
            <p>Got an idea? Launch your project and attract collaborators easily.</p>
          </a>
        </div>

        <div class="col-lg-4 col-md-6">
          <a href="/project/applications" class="nav-card">
            <div class="nav-card-icon">📄</div>
            <h3>My Applications</h3>
            <p>Keep track of where you've applied and see your progress.</p>
          </a>
        </div>

        <div class="col-lg-4 col-md-6">
          <a href="/project/show" class="nav-card">
            <div class="nav-card-icon">📂</div>
            <h3>My Projects</h3>
            <p>Manage your projects, review applicants, and update details.</p>
          </a>
        </div>

        <div class="col-lg-4 col-md-6">
          <a href="/user/profile" class="nav-card">
            <div class="nav-card-icon">👤</div>
            <h3>My Profile</h3>
            <p>Edit your profile, update your skills, and showcase achievements.</p>
          </a>
        </div>

        <div class="col-lg-4 col-md-6">
          <a href="/project/history" class="nav-card">
            <div class="nav-card-icon">🏛️</div>
            <h3>Project History</h3>
            <p>See your past creations and relive your project journey.</p>
          </a>
        </div>

      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer>
    <%- include('./partials/footer') %>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
